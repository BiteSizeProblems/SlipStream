<UserControl x:Class="SlipStream.Views.Multi.WeatherView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SlipStream.Views.Multi"
             mc:Ignorable="d" 
             Height="100" Width="400">
    <Border Style="{StaticResource MultiViewBorderTheme}">

        <Grid Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Weather: "
                       VerticalAlignment="Center"
                       FontSize="14" HorizontalAlignment="Right"/>
            
            <TextBlock Text="{Binding model.CurrentWeather}"
                       VerticalAlignment="Center" Grid.Column="1"
                       FontSize="14" HorizontalAlignment="Left"
                       Margin="12,0,0,0" Foreground="MediumAquamarine"/>

            <Image Source="{Binding model.CurrentWeatherIcon}" 
                   Margin="2" Grid.Column="2"
                   RenderOptions.BitmapScalingMode="HighQuality"/>

        </Grid>

    </Border>
</UserControl>
