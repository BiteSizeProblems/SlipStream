<UserControl x:Class="SlipStream.Views.Multi.SafetyCarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SlipStream.Views.Multi"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="600">
    <Border Background="{Binding model.SafetyCarColor}"
            CornerRadius="15">

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="{Binding model.SafetyCarStatus}"
                       VerticalAlignment="Center"
                       FontSize="36"
                       Margin="10,0,0,0"/>

            <Image Source="/Core/Images/safety_car.png" 
                   Margin="10" RenderOptions.BitmapScalingMode="HighQuality"
                   Grid.Column="1"/>

            <Image Source="/Core/Images/safety_car.png" 
                   Margin="10" RenderOptions.BitmapScalingMode="HighQuality"
                   Grid.Column="2"/>

        </Grid>
        
    </Border>
</UserControl>
