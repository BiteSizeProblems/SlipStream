<UserControl x:Class="SlipStream.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SlipStream.Views" xmlns:viewmodels="clr-namespace:SlipStream.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:HomeViewModel}"
             mc:Ignorable="d" 
             Height="600" Width="875">
    
    <Border Background="Transparent">
        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Label Content="Discover" 
                   Foreground="White" 
                   FontSize="50" 
                   FontWeight="Bold" 
                   VerticalAlignment="Center"/>

            <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="5,10,5,10">
                <StackPanel >
                    <Label Content="Connection Overview" 
                           Foreground="White" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           VerticalAlignment="Center"
                           Padding="10"/>
                    <TextBlock Text="{Binding ConnectionStatus}"
                               Padding="10"
                               Foreground="White"/>
                    <TextBlock Text="{Binding Test1}"
                               Padding="10"
                               Foreground="White"/>
                </StackPanel>
            </StackPanel>

        </Grid>
    </Border>

</UserControl>
